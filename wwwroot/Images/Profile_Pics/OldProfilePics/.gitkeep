(function () {
    'use strict';

    angular
        .module('app')
        .controller('_', _);

    _.$inject = ['$location'];

    function _($location) {
        /* jshint validthis:true */
        var vm = this;
        vm.title = '_';

        activate();

        function activate() { }
    }
})();
